# M6.18 MirrorEvaluator 归档

**任务**
- 新增 MirrorEvaluator 纯函数逻辑（x=0.5 镜像）。
- 输出 `mirrorApplied` 并向上游传递。

**实现摘要**
- `MirrorEvaluator` 提供 point/rect/pose/face 的镜像转换与 `MetricOutput` 镜像映射。
- 新增 `WithRefErrorEvaluator`：计算 normal 与 mirror 误差并返回 `MirrorEvaluationResult`。
- DebugTools 增加 `M6.18 MirrorEvaluatorProbe` 观察 `mirrorApplied` 输出。

**验证**
- 真机：DebugTools → `M6.18 MirrorEvaluatorProbe` 控制台输出 `mirrorApplied`。
- 验证结果：已通过（2026-02-24）。

**涉及文件**
- /Users/fanqie/Projects/opencode_just_photo/justphoto_opencode/Infrastructure/PoseSpec/MirrorEvaluator.swift
- /Users/fanqie/Projects/opencode_just_photo/justphoto_opencode/Features/Settings/DebugToolsScreen.swift

**Assumptions**
- UI 不显示 mirrorApplied，仅用于内部逻辑与日志。

请针对我刚刚创建/修改的 Swift 文件，编写对应的单元测试 (XCTest)。

**要求：**
1.  **覆盖率**：覆盖正常路径 (Happy Path) 和边缘情况 (Edge Cases)。
2.  **独立性**：测试用例应独立运行，不依赖外部复杂状态。
3.  **位置**：将测试代码写入项目的 `Tests` 目录下。

写完后，请告诉我如何在终端或 Xcode 中运行这些测试。

✅ 如何使用：完成一个功能模块（Feature）后，直接运行此 Skill。

请作为资深 iOS 架构师，分析我提供的错误信息或问题。

**执行步骤：**
1.  **定位**：根据错误信息，找出项目中对应的文件和代码行。
2.  **分析**：解释为什么会发生这个错误（是逻辑错误、语法错误还是环境问题？）。
3.  **方案**：提出 2-3 种修复方案，并推荐最稳健的一种（符合我们 Tech Stack 的）。
4.  **执行**：使用 `write` 或 `edit` 工具应用修复代码。
5.  **验证**：告诉我修复后该如何验证。

请一步步思考 (Think step by step)。

✅ 如何使用：当有报错时，复制报错信息，在 OpenCode 里按 Ctrl+K，选择 project:fix，然后粘贴报错信息。
